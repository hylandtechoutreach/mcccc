function calculate() {
	let x = 500;
	let y = 2;
	let z = 5;

	let g = x / y + z;
	let q = y + 10;
	
	if (q < y)
  {
		g = g - 5; y = y / y;
		q = q + 9 - 1 * 7 + 4 / 3 - 1;
	}
  else
  {
		x = x / 100 + 2 * 2;
		g = (x - y - z) * -1;
		q = 2 * ((g + x) / y);
	}
	
	console.log(q);
}

calculate();
